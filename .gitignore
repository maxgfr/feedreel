node_modules/
dist/
output/
cache/
logs/
.env
.env.*
!.env.example
*.db
*.db-journal
*.db-wal
*.db-shm
.DS_Store
remotion-bundle/
# Vendored fonts (downloaded at setup) — keep the dir, ignore the binaries
src/remotion/fonts/*.woff2
public/fonts/*.woff2
# CC0 music tracks (downloaded at setup, large)
assets/music/*.mp3
assets/music/*.wav
assets/music/*.m4a
assets/music/*.ogg
# Default shipped track (committed; other tracks are downloaded at setup)
!assets/music/morning-sky-by-roa-music.mp3
src/remotion/fonts/*.ttf
# Private RSS configs — never committed (only the example template is)
config/feedreel.yaml
config/feedreel.*.yaml
!config/feedreel.example.yaml
# Internal planning specs (development artifacts, not part of the shipped project)
docs/superpowers/
# Third-party installed agent skills (not part of this project)
.agents/
skills-lock.json
# Local Claude Code settings
.claude/settings.local.json
